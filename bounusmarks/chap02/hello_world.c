#include<stdio.h>

int main(int argc, char** argv)
{
	puts("helloworld!");
	//puts() function is very much similar to printf()
	//used to print the string on the console which is previously read by
	//using gets()or scanf() function.
	//puts() function returns an integer value representing the number of 
	//characters bing printed on the console
	//用于在console上打印字符串，这个字符串已通过gets()or scanf()读取
	return 0;
}
